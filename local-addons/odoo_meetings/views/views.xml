<odoo>
  <data>
    <!-- explicit list view definition -->

    <record id="view_calendar_event_type_tree" model="ir.ui.view">
        <field name="name">calendar.event.type</field>
        <field name="model">calendar.event.type</field>
        <field name="arch" type="xml">
            <tree string="Meeting Types" editable="bottom">
                <field name="name"/>
            </tree>
        </field>
    </record>

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="odoo_meetings.action_window">
      <field name="name">odoo_meetings window</field>
      <field name="res_model">odoo_meetings.odoo_meetings</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="odoo_meetings.action_server">
      <field name="name">odoo_meetings server</field>
      <field name="model_id" ref="model_odoo_meetings_odoo_meetings"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="odoo_meetings" id="odoo_meetings.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="odoo_meetings.menu_1" parent="odoo_meetings.menu_root"/>
    <menuitem name="Menu 2" id="odoo_meetings.menu_2" parent="odoo_meetings.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="odoo_meetings.menu_1_list" parent="odoo_meetings.menu_1"
              action="odoo_meetings.action_window"/>
    <menuitem name="Server to list" id="odoo_meetings" parent="odoo_meetings.menu_2"
              action="odoo_meetings.action_server"/>
-->
  </data>
</odoo>